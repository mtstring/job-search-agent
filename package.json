{
  "name": "job-search-agent",
  "private": true,
  "workspaces": ["packages/*", "packages/mcp-servers/*", "apps/*"],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "biome check .",
    "format": "biome format --write ."
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "bun-types": "^1.3.10",
    "turbo": "^2.5.3",
    "typescript": "^5.8.3"
  }
}
