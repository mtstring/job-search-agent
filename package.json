{
  "name": "job-search-agent",
  "private": true,
  "workspaces": ["packages/*", "packages/mcp-servers/*"],
  "scripts": {
    "lint": "biome check .",
    "format": "biome format --write .",
    "prepare": "git config core.hooksPath .githooks || true"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "bun-types": "^1.3.10",
    "typescript": "^5.8.3"
  }
}
